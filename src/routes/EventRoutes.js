const { Router } = require("express");
const EventController = require("./Controllers/EventController");

// const userRoutes = Router();
// userRoutes.post("/events", EventValidator.create, EventController.create);
// userRoutes.get("/events", EventValidator.read, EventController.read);
// userRoutes.delete("/events", EventValidator.delete, EventController.delete);
// userRoutes.put("/events", EventValidator.update, EventController.update);

module.exports = userRoutes;
