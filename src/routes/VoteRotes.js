const { Router } = require("express");
const VoteController = require("./Controllers/VoteController");

// const userRoutes = Router();
// userRoutes.post("/votes", VoteValidator.create, VoteController.create);
// userRoutes.get("/votes", VoteValidator.read, VoteController.read);
// userRoutes.delete("/votes", VoteValidator.delete, VoteController.delete);
// userRoutes.put("/votes", VoteValidator.update, VoteController.update);

module.exports = userRoutes;
