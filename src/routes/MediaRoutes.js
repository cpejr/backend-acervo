const { Router } = require("express");
const MediaController = require("./Controllers/MediaController");

// const userRoutes = Router();
// userRoutes.post("/medias", MediaValidator.create, MediaController.create);
// userRoutes.get("/medias", MediaValidator.read, MediaController.read);
// userRoutes.delete("/medias", MediaValidator.delete, MediaController.delete);
// userRoutes.put("/medias", MediaValidator.update, MediaController.update);

module.exports = userRoutes;
